spring:
  application:
    name: ProblemSolutionsLtd
  datasource:
    url: jdbc:postgresql://localhost:5432/problemsolution
    username: lex_luthor
    password: supermansux
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true